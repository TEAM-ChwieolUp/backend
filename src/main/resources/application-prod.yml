spring:
  config:
    activate:
      on-profile: prod

app:
  openapi:
    servers:
      - url: ${APP_OPENAPI_PROD_SERVER_URL:https://api.cheerup.example.com}
        description: Production server

cheerup:
  ai:
    retrospective:
      base-url: ${AI_RETROSPECTIVE_BASE_URL:http://202.31.135.91:8081}
