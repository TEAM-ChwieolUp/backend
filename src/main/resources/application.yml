spring:
  application:
    name: demo
  profiles:
    default: local
  security:
    oauth2:
      client:
        registration:
          google:
            scope:
              - openid
              - profile
              - email

server:
  forward-headers-strategy: framework

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

app:
  openapi:
    title: CheerUp API
    description: ChwieolUp backend API documentation
    version: v1
    servers:
      - url: http://localhost:8080
        description: Local server

cheerup:
  ai:
    server:
      connect-timeout: 1s
      read-timeout: 30s
      mail-stage-classify-path: /ai/mail/stage-classify
      kanban-move-recommend-path: /ai/kanban/move-recommend
      retrospective-questions-path: /ai/retrospective/questions
    retrospective:
      default-question-count: 4
      max-question-count: 10
      daily-limit: 50
